PORT=5000
DATABASE_URL=postgres://postgres:8888@localhost:5432/ayurveda_db
NODE_ENV=development

# Firebase Admin uses a service account. Put JSON content in one var:
FIREBASE_SERVICE_ACCOUNT_JSON=... (valid JSON)